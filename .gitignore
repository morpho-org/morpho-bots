# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnpm-store

# Local env files
.env
.env.*
!.env.example

# Local quoter-bot configuration (examples remain trackable)
**/quoter-bot.yaml
**/quoter-bot.yml
!**/quoter-bot.example.yaml
!**/quoter-bot.example.yml

# Legacy local configuration can still contain private keys after upgrading.
**/market-making.yaml
**/market-making.yml
!**/market-making.example.yaml
!**/market-making.example.yml


# Testing
coverage

# Build Outputs
build
dist

# Debug
*.log

# Misc
.DS_Store
*.pem

# tsbuildinfo
**/*.tsbuildinfo

# soltag generated ABI-type augmentation (regenerated by the `soltag` CLI before typecheck)
**/.soltag

# IDE preferences
.idea

# Claude settings (local overrides only)
.claude/settings.local.json
# Anchored deliberately. Unanchored, this pattern matches the `.claude/worktrees` segment at
# any depth, so knip — run from inside a nested worktree — sees every file as gitignored,
# finds no entry points, and reports the whole tree as unused. Git behavior is identical.
/.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/agent-memory/

# TIB review session ids (local pointers into ~/.codex/sessions, not shared history)
docs/decisions/*.sessions

# Devin local overrides (not shared)
.devin/config.local.json
