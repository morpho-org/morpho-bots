{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "generate": "node scripts/generate-abis.mjs && oxfmt src/abis.ts src/contracts.ts",
    "build": "soltag && bun run build.ts && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "typecheck": "soltag && tsc --noEmit"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "solc": "catalog:",
    "soltag": "catalog:",
    "typescript": "catalog:"
  }
}
