{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "generate": "bun run scripts/codegen.ts && biome check --write src/abis.ts src/contracts.ts",
    "build": "soltag && bun run scripts/build.ts && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "deploy:executor": "bun run build && bun run scripts/deploy-executor.ts",
    "typecheck": "soltag && tsc --noEmit",
    "format:sol": "forge fmt",
    "format:sol:check": "forge fmt --check"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "solc": "catalog:",
    "soltag": "catalog:",
    "typescript": "catalog:",
    "viem": "catalog:"
  }
}
