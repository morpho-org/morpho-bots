import { inMemoryRouterRemoved } from './mock-router'

/** Signals that genuine workflow coverage lives in `market-making.fork.e2e.test.ts`. */
export const marketMakingE2eUsesFork = inMemoryRouterRemoved
