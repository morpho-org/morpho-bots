import { describe, expect, test } from 'bun:test'
import type { TakeableOffer } from '../src/api'
import { matchBooks } from '../src/matching'
const ID=`0x${'11'.repeat(32)}` as const
function offer(buy:boolean,tick:bigint,units:bigint):TakeableOffer{return{marketId:ID,units,ratifierData:'0x',offer:{market:{chainId:8453n,midnight:`0x${'22'.repeat(20)}`,loanToken:`0x${'33'.repeat(20)}`,collateralParams:[],maturity:1n,rcfThreshold:0n,enterGate:`0x${'00'.repeat(20)}`,liquidatorGate:`0x${'00'.repeat(20)}`},buy,maker:`0x${'44'.repeat(20)}`,start:0n,expiry:1n,tick,group:`0x${'55'.repeat(32)}`,callback:`0x${'00'.repeat(20)}`,callbackData:'0x',receiverIfMakerIsSeller:`0x${'00'.repeat(20)}`,ratifier:`0x${'66'.repeat(20)}`,reduceOnly:false,maxUnits:units,maxAssets:0n,continuousFeeCap:1n}}}
describe('matchBooks',()=>{test('pairs best crossed ask and bid at the smaller size',()=>{const [m]=matchBooks([offer(false,9n,8n),offer(false,5n,4n)],[offer(true,7n,6n)]);expect(m?.ask.offer.tick).toBe(5n);expect(m?.bid.offer.tick).toBe(7n);expect(m?.units).toBe(4n)});test('does not pair a locked or uncrossed book',()=>{expect(matchBooks([offer(false,7n,1n)],[offer(true,7n,1n)])).toEqual([]);expect(matchBooks([offer(false,8n,1n)],[offer(true,7n,1n)])).toEqual([])});test('walks both sets without overfilling offers',()=>{const matches=matchBooks([offer(false,5n,2n),offer(false,6n,5n)],[offer(true,8n,4n),offer(true,7n,4n)],3);expect(matches.map(m=>m.units)).toEqual([2n,2n,3n])})})
